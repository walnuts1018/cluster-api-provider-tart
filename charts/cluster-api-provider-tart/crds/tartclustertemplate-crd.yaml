apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  labels:
    cluster.x-k8s.io/provider: infrastructure-tart
    cluster.x-k8s.io/v1beta1: v1alpha1
    cluster.x-k8s.io/v1beta2: v1alpha1
  name: tartclustertemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: TartClusterTemplate
    listKind: TartClusterTemplateList
    plural: tartclustertemplates
    singular: tartclustertemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TartClusterTemplate is the Schema for the tartclustertemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TartClusterTemplate.
            properties:
              template:
                description: template is the TartCluster object template used by Cluster
                  API controllers.
                properties:
                  metadata:
                    description: metadata is applied to the generated TartCluster.
                    type: object
                  spec:
                    description: spec defines the desired state of the generated TartCluster.
                    properties:
                      controlPlaneEndpoint:
                        description: controlPlaneEndpoint represents the endpoint
                          for the control plane for this cluster.
                        properties:
                          host:
                            description: host is the hostname or IP address of the
                              control plane endpoint.
                            type: string
                          port:
                            description: port is the port of the control plane endpoint.
                            format: int32
                            maximum: 65535
                            minimum: 0
                            type: integer
                        required:
                        - host
                        type: object
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true

