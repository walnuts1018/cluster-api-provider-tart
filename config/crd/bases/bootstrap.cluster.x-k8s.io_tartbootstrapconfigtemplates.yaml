---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  labels:
    cluster.x-k8s.io/v1beta2: v1alpha1
  name: tartbootstrapconfigtemplates.bootstrap.cluster.x-k8s.io
spec:
  group: bootstrap.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: TartBootstrapConfigTemplate
    listKind: TartBootstrapConfigTemplateList
    plural: tartbootstrapconfigtemplates
    shortNames:
    - tbcfgt
    singular: tartbootstrapconfigtemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TartBootstrapConfigTemplateはtartbootstrapconfigtemplates APIのschemaである。
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TartBootstrapConfigTemplateSpecはTartBootstrapConfigTemplateのdesired
              stateを定義する。
            properties:
              template:
                description: TartBootstrapConfigTemplateResourceはtemplateからTartBootstrapConfigを作成するためのdataを定義する。
                properties:
                  metadata:
                    description: ObjectMetaは生成されるBootstrapConfigとそのSecretへ伝播する。
                    minProperties: 1
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: TartBootstrapConfigTemplateResourceSpecはtemplateから作成される具体的なBootstrapConfigで共有するimmutable
                      Secret inputを定義する。
                    properties:
                      configPatchesSecretRef:
                        description: |-
                          LocalObjectReference contains enough information to let you locate the
                          referenced object inside the same namespace.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      updatePolicy:
                        description: updatePolicyは生成されるTartBootstrapConfigのconfiguration
                          update policyを共有する。
                        properties:
                          configuration:
                            default: StagedReboot
                            description: configurationはeffective Talos machine configurationの適用strategyを指定する。既定値はStagedRebootである。
                            enum:
                            - StagedReboot
                            - ApplyOnly
                            type: string
                        type: object
                    type: object
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
