---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: tartbootstrapconfigtemplates.bootstrap.cluster.x-k8s.io
spec:
  group: bootstrap.cluster.x-k8s.io
  names:
    kind: TartBootstrapConfigTemplate
    listKind: TartBootstrapConfigTemplateList
    plural: tartbootstrapconfigtemplates
    singular: tartbootstrapconfigtemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TartBootstrapConfigTemplate is the Schema for the tartbootstrapconfigtemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TartBootstrapConfigTemplateSpec defines the desired state
              of a TartBootstrapConfigTemplate.
            properties:
              template:
                description: |-
                  TartBootstrapConfigTemplateResource describes the data needed to create a
                  TartBootstrapConfig from a template.
                properties:
                  spec:
                    description: |-
                      TartBootstrapConfigSpec defines the desired state of a TartBootstrapConfig. All
                      user-owned raw Talos configuration patches are supplied via an immutable
                      Secret-backed input; no raw patch is ever stored inline in this Spec.
                    properties:
                      configSecretRef:
                        description: |-
                          configSecretRef optionally references an immutable Secret holding user-owned raw
                          Talos configuration patches. The referenced Secret must set immutable: true.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
