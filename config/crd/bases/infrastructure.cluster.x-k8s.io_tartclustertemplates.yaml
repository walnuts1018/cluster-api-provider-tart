---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: tartclustertemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: TartClusterTemplate
    listKind: TartClusterTemplateList
    plural: tartclustertemplates
    singular: tartclustertemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TartClusterTemplate is the Schema for the tartclustertemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TartClusterTemplateSpec defines the desired state of a TartClusterTemplate.
            properties:
              template:
                description: TartClusterTemplateResource describes the data needed
                  to create a TartCluster from a template.
                properties:
                  spec:
                    description: TartClusterSpec defines the desired state of a TartCluster.
                    properties:
                      id:
                        description: |-
                          id is an immutable random UUID that identifies this workload cluster independently
                          of Cluster.metadata.uid. It is generated once by the controller on first
                          non-dry-run creation and is never regenerated for the same object; a cluster
                          recreated under the same name gets a new id and must not reuse old secret bundles
                          or Retained Hosts.
                        type: string
                        x-kubernetes-validations:
                        - message: id is immutable
                          rule: self == oldSelf
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
