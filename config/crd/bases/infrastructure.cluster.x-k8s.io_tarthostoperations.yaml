---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tarthostoperations.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: TartHostOperation
    listKind: TartHostOperationList
    plural: tarthostoperations
    singular: tarthostoperation
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Operation type
      jsonPath: .spec.type
      name: Type
      type: string
    - description: Operation phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: Target TartHost
      jsonPath: .spec.hostRef.name
      name: Host
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              deadline:
                description: deadline is the absolute time after which the operation
                  must stop retrying.
                format: date-time
                type: string
              desiredObjectsDigest:
                description: desiredObjectsDigest identifies the canonical desired
                  Kubernetes objects that produced the Plan.
                pattern: ^sha256:[0-9a-f]{64}$
                type: string
              hostRef:
                description: hostRef identifies the physical host locked by this operation.
                properties:
                  name:
                    description: name is the name of the referenced resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: namespace is the namespace of the referenced resource.
                    minLength: 1
                    type: string
                  uid:
                    description: uid is the immutable UID of the referenced resource.
                    type: string
                required:
                - name
                - namespace
                - uid
                type: object
              machineRef:
                description: |-
                  machineRef identifies the TartMachine associated with this operation.
                  It is omitted only for a manually requested WipeAll operation.
                properties:
                  name:
                    description: name is the name of the referenced resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: namespace is the namespace of the referenced resource.
                    minLength: 1
                    type: string
                  uid:
                    description: uid is the immutable UID of the referenced resource.
                    type: string
                required:
                - name
                - namespace
                - uid
                type: object
              operationID:
                description: operationID is the immutable UUID used to make external
                  side effects idempotent.
                format: uuid
                type: string
              planDigest:
                description: planDigest is the digest of the canonical signed Plan.
                pattern: ^sha256:[0-9a-f]{64}$
                type: string
              targetArtifactGeneration:
                description: targetArtifactGeneration is required for update operations.
                format: int64
                minimum: 1
                type: integer
              targetDistributionVersion:
                description: targetDistributionVersion is the Kubernetes distribution
                  version requested by the Plan.
                maxLength: 128
                minLength: 1
                type: string
              targetImageDigest:
                description: targetImageDigest is required for update operations.
                pattern: ^sha256:[0-9a-f]{64}$
                type: string
              targetSlot:
                description: targetSlot is required for update operations.
                enum:
                - A
                - B
                type: string
              type:
                description: type identifies the workflow executed by this operation.
                enum:
                - Provision
                - Update
                - Rollback
                - Clean
                - WipeAll
                - Recovery
                type: string
              updateClass:
                description: updateClass is required for update operations.
                enum:
                - OSOnly
                - KubernetesBinary
                - StateMigration
                type: string
            required:
            - deadline
            - desiredObjectsDigest
            - hostRef
            - operationID
            - planDigest
            - type
            type: object
          status:
            properties:
              agentSequence:
                description: agentSequence is the largest accepted monotonically increasing
                  Agent report sequence.
                format: int64
                minimum: 0
                type: integer
              attempt:
                description: attempt is the number of attempts made for the current
                  step.
                format: int32
                minimum: 0
                type: integer
              bootstrapDelivered:
                description: |-
                  bootstrapDelivered indicates that Bootstrap Data was claimed once for this operation.
                  Issuing a new Session Token does not clear this operation-level replay guard.
                type: boolean
              completedSteps:
                description: completedSteps is the set of successfully completed idempotent
                  steps.
                items:
                  type: string
                maxItems: 128
                type: array
                x-kubernetes-list-type: set
              conditions:
                description: conditions represent the current state of the TartHostOperation.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastBootReport:
                description: lastBootReport is the latest authenticated OS boot observation.
                properties:
                  activeSlot:
                    description: activeSlot is the slot observed by the operating
                      system.
                    enum:
                    - A
                    - B
                    type: string
                  artifactGeneration:
                    description: artifactGeneration is the generation observed in
                      the active slot.
                    format: int64
                    minimum: 1
                    type: integer
                  bootID:
                    description: bootID identifies one boot of the installed operating
                      system.
                    maxLength: 128
                    minLength: 1
                    type: string
                  bootstrapApplied:
                    description: bootstrapApplied indicates that the Bootstrap success
                      marker exists in State.
                    type: boolean
                  dataMounted:
                    description: dataMounted indicates that the required Data filesystem
                      is mounted.
                    type: boolean
                  reportedAt:
                    description: reportedAt is the controller receipt time and is
                      not supplied by the Agent.
                    format: date-time
                    type: string
                  stateMounted:
                    description: stateMounted indicates that the required State filesystem
                      is mounted.
                    type: boolean
                required:
                - activeSlot
                - artifactGeneration
                - bootID
                - bootstrapApplied
                - dataMounted
                - reportedAt
                - stateMounted
                type: object
              lifecyclePhase:
                description: lifecyclePhase identifies the current typed distribution
                  lifecycle step.
                enum:
                - Preflight
                - Snapshot
                - Apply
                - Verify
                type: string
              observedGeneration:
                description: observedGeneration is the last spec generation reconciled
                  into status.
                format: int64
                type: integer
              phase:
                description: phase is the durable resume point of the operation.
                enum:
                - Pending
                - PreparingBoot
                - WaitingForAgent
                - Writing
                - Verifying
                - BootTrial
                - AwaitingHealth
                - DistributionUpdating
                - RollingBack
                - Succeeded
                - Failed
                - RecoveryRequired
                type: string
              sessionAuthenticationFailures:
                description: sessionAuthenticationFailures is the number of rejected
                  authentication attempts.
                format: int32
                maximum: 5
                minimum: 0
                type: integer
              sessionTokenConsumed:
                description: sessionTokenConsumed indicates that Bootstrap delivery
                  was claimed and may not be replayed.
                type: boolean
              sessionTokenExpiresAt:
                description: sessionTokenExpiresAt is the immutable expiry of the
                  active Session Token.
                format: date-time
                type: string
              sessionTokenHash:
                description: |-
                  sessionTokenHash is the SHA-256 hash of the active Session Token.
                  The plaintext token is never stored in Kubernetes.
                pattern: ^[0-9a-f]{64}$
                type: string
              snapshotRef:
                description: snapshotRef identifies the snapshot required to recover
                  a state migration.
                properties:
                  name:
                    description: name is the name of the referenced resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: namespace is the namespace of the referenced resource.
                    minLength: 1
                    type: string
                  uid:
                    description: uid is the immutable UID of the referenced resource.
                    type: string
                required:
                - name
                - namespace
                - uid
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
