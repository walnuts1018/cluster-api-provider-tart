---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tarthosts.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: TartHost
    listKind: TartHostList
    plural: tarthosts
    singular: tarthost
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Host allocation state
      jsonPath: .status.state
      name: State
      type: string
    - description: NIC MAC address
      jsonPath: .spec.macAddress
      name: MAC Address
      type: string
    - description: PXE boot NIC MAC address
      jsonPath: .spec.bootMacAddress
      name: Boot MAC Address
      type: string
    - description: Assigned TartMachine
      jsonPath: .status.machineRef.name
      name: Machine
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TartHost is the Schema for the tarthosts API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TartHost
            properties:
              bootMacAddress:
                description: bootMACAddress is the PXE boot NIC MAC address when it
                  differs from macAddress.
                pattern: ^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$
                type: string
              macAddress:
                description: macAddress is the managed NIC MAC address that identifies
                  the physical host.
                pattern: ^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$
                type: string
            required:
            - macAddress
            type: object
          status:
            description: status defines the observed state of TartHost
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the TartHost resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              machineRef:
                description: machineRef references the TartMachine currently reserving
                  this host.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              observedGeneration:
                description: observedGeneration is the last spec generation reconciled
                  into status.
                format: int64
                type: integer
              state:
                description: state is the host allocation state.
                enum:
                - Available
                - Reserved
                - Provisioning
                - Provisioned
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - additionalPrinterColumns:
    - description: Host lifecycle phase
      jsonPath: .status.phase
      name: Phase
      type: string
    - description: CPU architecture
      jsonPath: .spec.architecture
      name: Architecture
      type: string
    - description: Assigned TartMachine
      jsonPath: .spec.consumerRef.name
      name: Consumer
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              architecture:
                description: architecture is the CPU architecture of the physical
                  host.
                enum:
                - amd64
                - arm64
                type: string
              consumerRef:
                description: consumerRef identifies the TartMachine currently assigned
                  to this host.
                properties:
                  name:
                    description: name is the name of the referenced resource.
                    minLength: 1
                    type: string
                  namespace:
                    description: namespace is the namespace of the referenced resource.
                    minLength: 1
                    type: string
                  uid:
                    description: uid is the immutable UID of the referenced resource.
                    type: string
                required:
                - name
                - namespace
                - uid
                type: object
              firmware:
                description: firmware is the firmware interface used to boot the host.
                enum:
                - UEFI
                - LegacyBIOS
                - RaspberryPiEEPROM
                type: string
              identifiers:
                description: identifiers contains stable identifiers for the physical
                  host.
                properties:
                  bootMACAddress:
                    description: bootMACAddress is the MAC address used for network
                      boot.
                    pattern: ^([0-9A-Fa-f]{2}:){5}[0-9A-Fa-f]{2}$
                    type: string
                  systemUUID:
                    description: systemUUID is the firmware-reported system UUID.
                    maxLength: 128
                    minLength: 1
                    type: string
                required:
                - bootMACAddress
                type: object
              management:
                description: management selects the power and boot drivers used for
                  this host.
                properties:
                  bootDriver:
                    description: bootDriver is the configured boot driver name.
                    maxLength: 64
                    minLength: 1
                    type: string
                  credentialsSecretRef:
                    description: credentialsSecretRef references credentials required
                      by the configured drivers.
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  powerDriver:
                    description: powerDriver is the configured power driver name.
                    maxLength: 64
                    minLength: 1
                    type: string
                  redfish:
                    description: redfish contains BMC connection settings used when
                      either driver is Redfish.
                    properties:
                      caBundleSecretRef:
                        description: caBundleSecretRef references a Secret whose ca.crt
                          key extends the TLS trust roots.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      endpoint:
                        description: endpoint is the Redfish service root URL exposed
                          by the BMC.
                        pattern: ^https://.+$
                        type: string
                      preferredBootTransport:
                        description: preferredBootTransport forces a specific Redfish
                          boot transport when set.
                        enum:
                        - IPXE
                        - RedfishPXE
                        - RedfishHTTPBoot
                        - RedfishVirtualMedia
                        type: string
                      spkiPins:
                        description: spkiPins contains acceptable SHA-256 SPKI pins
                          for the BMC certificate chain.
                        items:
                          type: string
                        type: array
                        x-kubernetes-list-type: set
                    required:
                    - endpoint
                    type: object
                required:
                - bootDriver
                - powerDriver
                type: object
              platformProfile:
                description: platformProfile identifies the versioned platform configuration
                  required by this host.
                maxLength: 128
                minLength: 1
                type: string
              rootDeviceHints:
                description: rootDeviceHints identifies the disk that may be modified
                  by the Provisioning Agent.
                properties:
                  deviceName:
                    description: deviceName is a stable /dev/disk/by-id path.
                    pattern: ^/dev/disk/by-id/.+$
                    type: string
                  minSizeBytes:
                    description: minSizeBytes is the minimum acceptable disk capacity
                      in bytes.
                    format: int64
                    minimum: 1
                    type: integer
                  serialNumber:
                    description: serialNumber is the disk serial number reported by
                      the operating system.
                    maxLength: 256
                    minLength: 1
                    type: string
                  wwn:
                    description: wwn is the disk World Wide Name.
                    maxLength: 256
                    minLength: 1
                    type: string
                required:
                - minSizeBytes
                type: object
            required:
            - architecture
            - firmware
            - identifiers
            - management
            - platformProfile
            - rootDeviceHints
            type: object
          status:
            properties:
              capabilities:
                description: capabilities contains only operations implemented by
                  the configured drivers.
                items:
                  enum:
                  - PowerOn
                  - PowerOff
                  - ObservePowerState
                  - SetNextBoot
                  - VirtualMedia
                  type: string
                type: array
                x-kubernetes-list-type: set
              conditions:
                description: conditions represent the current state of the TartHost.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              inventory:
                description: inventory contains the most recent hardware inventory
                  reported by the Provisioning Agent.
                properties:
                  observedAt:
                    description: observedAt is the time at which the inventory was
                      reported.
                    format: date-time
                    type: string
                  rootDisk:
                    description: rootDisk contains the observed identity and size
                      of the selected root disk.
                    properties:
                      deviceName:
                        description: deviceName is the stable path reported for the
                          disk.
                        type: string
                      serialNumber:
                        description: serialNumber is the reported disk serial number.
                        type: string
                      sizeBytes:
                        description: sizeBytes is the reported disk capacity in bytes.
                        format: int64
                        type: integer
                      wwn:
                        description: wwn is the reported disk World Wide Name.
                        type: string
                    type: object
                type: object
              lastStablePhase:
                description: lastStablePhase is the stable phase to restore after
                  an Error.
                enum:
                - Available
                - Provisioned
                - Retained
                - Detached
                type: string
              observedGeneration:
                description: observedGeneration is the last spec generation reconciled
                  into status.
                format: int64
                type: integer
              phase:
                description: phase is the current host lifecycle phase.
                enum:
                - Available
                - Reserved
                - Provisioning
                - Provisioned
                - Updating
                - Cleaning
                - Retained
                - Detached
                - RecoveryRequired
                - Error
                type: string
              powerState:
                description: powerState is the last state reported by a PowerState
                  observer.
                enum:
                - "On"
                - "Off"
                - Unknown
                type: string
              reachability:
                description: reachability is the last network observation and does
                  not imply a power state.
                enum:
                - Reachable
                - Unreachable
                - Unknown
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
    subresources:
      status: {}
