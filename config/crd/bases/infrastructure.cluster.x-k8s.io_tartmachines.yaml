---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: tartmachines.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: TartMachine
    listKind: TartMachineList
    plural: tartmachines
    singular: tartmachine
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Ready status
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: Assigned TartHost
      jsonPath: .status.hostRef.name
      name: Host
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TartMachine is the Schema for the tartmachines API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TartMachine
            properties:
              bootstrap:
                default: {}
                description: bootstrap configures how bootstrap data is passed to
                  the booted OS or installer.
                properties:
                  format:
                    default: NoCloud
                    description: |-
                      format selects how bootstrap data is exposed to the booted OS or installer.
                      Defaults to NoCloud for the default Ubuntu kubeadm bootstrap flow when omitted.
                    enum:
                    - Talos
                    - NoCloud
                    type: string
                type: object
              failureDomain:
                description: |-
                  failureDomain specifies the failure domain for the machine.
                  Must comply with the failure domain specified in the core Machine object.
                type: string
              image:
                description: image is the raw disk image URL or a path served by the
                  assets server.
                minLength: 1
                type: string
              kernelParams:
                description: kernelParams are passed from iPXE to the OS kernel.
                items:
                  maxLength: 2048
                  minLength: 1
                  type: string
                maxItems: 100
                type: array
              providerID:
                description: providerID is the infrastructure provider ID reported
                  back to the CAPI Machine.
                type: string
            required:
            - image
            type: object
          status:
            description: status defines the observed state of TartMachine
            properties:
              addresses:
                description: addresses holds the IP addresses of the provisioned machine.
                items:
                  description: TartMachineAddress defines the IP address of the machine.
                  properties:
                    address:
                      description: address is the IP address of the machine.
                      type: string
                    type:
                      description: type is the type of the address.
                      type: string
                  type: object
                type: array
              bootstrapSecretName:
                description: bootstrapSecretName is the Secret name that stores bootstrap
                  data served once to the host.
                type: string
              conditions:
                description: |-
                  conditions represent the current state of the TartMachine resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Available": the resource is fully functional
                  - "Progressing": the resource is being created or updated
                  - "Degraded": the resource failed to reach or maintain its desired state

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              consumedBootstrapTokenHash:
                description: consumedBootstrapTokenHash stores the SHA-256 hash of
                  the consumed bootstrap token for non-secret NoCloud metadata validation.
                type: string
              hostRef:
                description: hostRef references the assigned TartHost.
                properties:
                  apiVersion:
                    description: API version of the referent.
                    type: string
                  fieldPath:
                    description: |-
                      If referring to a piece of an object instead of an entire object, this string
                      should contain a valid JSON/Go field access statement, such as desiredState.manifest.containers[2].
                      For example, if the object reference is to a container within a pod, this would take on a value like:
                      "spec.containers{name}" (where "name" refers to the name of the container that triggered
                      the event) or if no container name is specified "spec.containers[2]" (container with
                      index 2 in this pod). This syntax is chosen only to have some well-defined way of
                      referencing a part of an object.
                    type: string
                  kind:
                    description: |-
                      Kind of the referent.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                    type: string
                  name:
                    description: |-
                      Name of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces/
                    type: string
                  resourceVersion:
                    description: |-
                      Specific resourceVersion to which this reference is made, if any.
                      More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
                    type: string
                  uid:
                    description: |-
                      UID of the referent.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#uids
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              initialization:
                description: initialization holds the initialization state of the
                  TartMachine.
                properties:
                  provisioned:
                    description: provisioned indicates that the infrastructure is
                      provisioned and ready.
                    type: boolean
                type: object
              observedGeneration:
                description: observedGeneration is the last spec generation reconciled
                  into status.
                format: int64
                type: integer
              provisioningStartTime:
                description: provisioningStartTime is the start of the WoL and metadata
                  access window.
                format: date-time
                type: string
              ready:
                description: ready indicates whether the infrastructure resource is
                  ready for the CAPI Machine.
                type: boolean
              tokenExpiresAt:
                description: tokenExpiresAt is the deadline for serving bootstrap
                  data.
                format: date-time
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
