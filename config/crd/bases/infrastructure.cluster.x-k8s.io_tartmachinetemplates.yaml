---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: tartmachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: TartMachineTemplate
    listKind: TartMachineTemplateList
    plural: tartmachinetemplates
    singular: tartmachinetemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TartMachineTemplate is the Schema for the tartmachinetemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TartMachineTemplateSpec defines the desired state of a TartMachineTemplate.
            properties:
              template:
                description: TartMachineTemplateResource describes the data needed
                  to create a TartMachine from a template.
                properties:
                  spec:
                    description: TartMachineSpec defines the desired state of a TartMachine.
                    properties:
                      hostRef:
                        description: hostRef explicitly selects a TartHost. Immutable
                          after the Host claim succeeds.
                        properties:
                          name:
                            default: ""
                            description: |-
                              Name of the referent.
                              This field is effectively required, but due to backwards compatibility is
                              allowed to be empty. Instances of this type with an empty value here are
                              almost certainly wrong.
                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            type: string
                        type: object
                        x-kubernetes-map-type: atomic
                      hostSelector:
                        description: |-
                          hostSelector deterministically narrows Host allocation when hostRef is not set.
                          Changing it after claim is a safe-stop, not an in-place update.
                        properties:
                          architecture:
                            type: string
                          failureDomain:
                            type: string
                          matchLabels:
                            additionalProperties:
                              type: string
                            type: object
                        type: object
                      providerID:
                        description: |-
                          providerID is derived deterministically from the claimed TartHost.spec.id as
                          tart://host/<TartHost.spec.id> once Host allocation succeeds.
                        type: string
                      talosImage:
                        description: talosImage is the desired Talos OS version and
                          schematic identity.
                        properties:
                          schematicID:
                            description: |-
                              schematicID is the Image Factory schematic identifier, which also determines the
                              installed system extension set.
                            type: string
                          version:
                            description: version is the desired Talos OS version.
                            type: string
                        required:
                        - schematicID
                        - version
                        type: object
                    required:
                    - talosImage
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
