---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: tartmachinetemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: TartMachineTemplate
    listKind: TartMachineTemplateList
    plural: tartmachinetemplates
    singular: tartmachinetemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TartMachineTemplate is the Schema for the tartmachinetemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TartMachineTemplate.
            properties:
              template:
                description: template is the TartMachine object template used by Cluster
                  API controllers.
                properties:
                  metadata:
                    description: metadata is applied to the generated TartMachine.
                    type: object
                  spec:
                    description: spec defines the desired state of the generated TartMachine.
                    properties:
                      bootstrap:
                        default: {}
                        description: bootstrap configures how bootstrap data is passed
                          to the booted OS or installer.
                        properties:
                          format:
                            default: NoCloud
                            description: |-
                              format selects how bootstrap data is exposed to the booted OS or installer.
                              Defaults to NoCloud for the default Ubuntu kubeadm bootstrap flow when omitted.
                            enum:
                            - Talos
                            - NoCloud
                            - Preseed
                            - Raw
                            type: string
                        type: object
                      failureDomain:
                        description: |-
                          failureDomain specifies the failure domain for the machine.
                          Must comply with the failure domain specified in the core Machine object.
                        type: string
                      image:
                        description: image is the boot OS image URL or a path served
                          by the assets server.
                        minLength: 1
                        type: string
                      initrd:
                        description: initrd is the boot initrd image URL or a path
                          served by the assets server.
                        minLength: 1
                        type: string
                      kernelParams:
                        description: kernelParams are passed from iPXE to the OS kernel.
                        items:
                          maxLength: 2048
                          minLength: 1
                          type: string
                        maxItems: 100
                        type: array
                      providerID:
                        description: providerID is the infrastructure provider ID
                          reported back to the CAPI Machine.
                        type: string
                    required:
                    - image
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
  - name: v1beta1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              template:
                description: template is the TartMachine object template used by Cluster
                  API controllers.
                properties:
                  metadata:
                    description: metadata is applied to the generated TartMachine.
                    minProperties: 1
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: spec defines fields copied to a generated TartMachine.
                    properties:
                      deletionPolicy:
                        description: deletionPolicy controls how State and Data are
                          handled when the machine is deleted.
                        enum:
                        - WipeAll
                        - RetainData
                        - RetainState
                        type: string
                      hostSelector:
                        description: hostSelector selects TartHosts by label.
                        properties:
                          matchLabels:
                            additionalProperties:
                              type: string
                            description: matchLabels selects hosts containing all
                              listed labels.
                            maxProperties: 64
                            type: object
                        type: object
                      image:
                        description: image identifies the digest-pinned OS artifact
                          to install.
                        properties:
                          ref:
                            description: ref is a digest-pinned OCI artifact reference.
                            pattern: ^oci://[^@[:space:]]+@sha256:[0-9a-f]{64}$
                            type: string
                        required:
                        - ref
                        type: object
                      platformProfile:
                        description: platformProfile identifies the versioned platform
                          configuration required by the machine.
                        maxLength: 128
                        minLength: 1
                        type: string
                      updatePolicy:
                        default:
                          mode: Replace
                        description: updatePolicy selects replacement or experimental
                          in-place updates.
                        properties:
                          mode:
                            default: Replace
                            description: mode selects machine replacement or an experimental
                              in-place update.
                            enum:
                            - Replace
                            - InPlace
                            type: string
                        type: object
                    required:
                    - deletionPolicy
                    - image
                    - platformProfile
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        required:
        - spec
        type: object
    served: false
    storage: false
