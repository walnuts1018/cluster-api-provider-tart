# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default/bootstrap
resources:
- ../bases/bootstrap.cluster.x-k8s.io_tartbootstrapconfigs.yaml
- ../bases/bootstrap.cluster.x-k8s.io_tartbootstrapconfigtemplates.yaml
# +kubebuilder:scaffold:crdkustomizeresource

labels:
- includeSelectors: true
  pairs:
    cluster.x-k8s.io/provider: bootstrap-tart
