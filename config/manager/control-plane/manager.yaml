apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: control-plane-controller-manager
    app.kubernetes.io/name: cluster-api-provider-tart
    app.kubernetes.io/managed-by: kustomize
    cluster.x-k8s.io/provider: control-plane-tart
  # "system" は Kustomize の namePrefix (cluster-api-provider-tart-) により
  # "cluster-api-provider-tart-system" に変換されるプレースホルダーです。
  name: system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: control-plane-controller-manager
  namespace: system
  labels:
    control-plane: control-plane-controller-manager
    app.kubernetes.io/name: cluster-api-provider-tart
    app.kubernetes.io/managed-by: kustomize
    cluster.x-k8s.io/provider: control-plane-tart
spec:
  selector:
    matchLabels:
      control-plane: control-plane-controller-manager
      app.kubernetes.io/name: cluster-api-provider-tart
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        control-plane: control-plane-controller-manager
        app.kubernetes.io/name: cluster-api-provider-tart
        cluster.x-k8s.io/provider: control-plane-tart
    spec:
      securityContext:
        # Projects are configured by default to adhere to the "restricted" Pod Security Standards.
        # This ensures that deployments meet the highest security requirements for Kubernetes.
        # For more details, see: https://kubernetes.io/docs/concepts/security/pod-security-standards/#restricted
        runAsNonRoot: true
        # distroless nonrootイメージのUSERはnonroot(名前)であり、numeric UIDでの
        # 検証をkubeletが行えないためrunAsUserを明示する(distroless nonrootの既定UID)。
        runAsUser: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
      - command:
        - /manager
        args:
          - --leader-elect
          - --health-probe-bind-address=:8081
        image: control-plane-controller:latest
        name: manager
        ports:
        - containerPort: 9443
          name: runtime-ext
          protocol: TCP
        securityContext:
          readOnlyRootFilesystem: true
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - "ALL"
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
      serviceAccountName: control-plane-controller-manager
      terminationGracePeriodSeconds: 10
