apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    cluster.x-k8s.io/provider: bootstrap-tart
  name: bootstrap-tart-metrics-reader
rules:
- nonResourceURLs:
  - "/metrics"
  verbs:
  - get
